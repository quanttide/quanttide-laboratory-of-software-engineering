# 路线图

实验室的探索方向。状态在 STATUS.md 中记录。

## 探索方向

### 证据链组织

核心问题：如何组织证据链才能最大化 LLM 的"意外发现"概率？

已验证：
- ✅ 证据完整性：全代码 / 追溯链 > 单行
- ✅ 证据排序：正向链关注结构，反向链关注正确性

待验证：
- [ ] 不同粒度的追溯链（2 层 vs 5 层 vs 全路径）对发现率的影响
- [ ] 多变量对比呈现（price 和 qty 并列）是否比单变量追溯更容易发现模式
- [ ] 反面案例：什么情况下完整代码反而让 LLM 忽略关键问题？

### LLM 误报识别和发现 bug 是同一能力

假设：`sum += v` dismiss 和 `parts[2]` 发现用了同一个机制——LLM 综合上下文判断。如果成立，则 `--llm` 不需要预设分析维度，一个通用 prompt 即可覆盖 dismiss 和 discover 两种场景。

可验证：在真实项目上，通用 prompt vs 专用 prompt 对比误报识别率和 bug 发现率。

### reflect 命令原型

定向分析，不跑 review。提供 reflect 工具的 CLI 入口。

## 要做什么

- **做工具，不做框架** — reflect 是嵌入 review 流程的分析工具链，不引入框架层抽象。每个工具独立可调，组合由用户决定。
- **做 CLI，不做平台** — 所有能力通过 CLI 入口暴露，不依赖容器编排或 agent 运行时。安装即用，零依赖。
- **做独立发布** — 生产 CLI 不依赖实验室进度，各自独立迭代。实验室验证成熟的能力才迁移到生产代码。
