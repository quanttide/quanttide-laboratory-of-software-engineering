# 实验室

reflect 工具 + LLM 组合的实验场。验证模式、评估质量、然后决定是否整合到正式 CLI。

## 当前工具

| 工具 | 状态 | 说明 |
|------|------|------|
| `backward_slice` | ✅ 稳定 | 从行号追溯变量定义 |
| `forward_slice` | ✅ 稳定 | 从定义找所有引用 |
| `flatten_stmts` | ✅ 稳定 | 展平函数体 |
| `call_graph` | ✅ 稳定 | 函数级调用关系 |
| `type_info` | ✅ 稳定 | 变量类型注解 |
| `trace_variable` | ⚠️ 基本路径 | 跨函数不工作 |
| `enhance_finding` (LLM) | ✅ 可用 | DeepSeek + Vault |
| `compute_confidence` | ✅ 可用 | 证据锚定率置信度 |

## 已验证

- LLM 在单函数范围内 + 精确证据 → 有效
- LLM 跨文件/跨模块 → 不可靠（空洞架构建议）
- 置信度计算：基于证据锚定率的确定性指标（高/中/低）
- 证据完整性实验：只给一行证据 LLM 会跑偏，给全代码或追溯链 LLM 自主发现 bug
- `parts[2]` 案例：LLM 发现未预设的越界 bug（非 prompt 设计结果）

## 探索方向

### 证据链组织

核心问题：如何组织证据链才能最大化 LLM 的"意外发现"概率？

初步发现：
- A（单行）→ ❌ LLM 跑偏
- B（完整代码）→ ✅ 发现 bug + 置信度高
- C（追溯链）→ ✅ 发现 bug + 置信度高
- B 和 C 效果接近，但 C 的证据更精炼

待验证：
- 不同粒度的追溯链（2 层 vs 5 层 vs 全路径）对发现率的影响
- 多变量对比呈现（price 和 qty 并列）是否比单变量追溯更容易发现模式
- 反面案例：什么情况下完整代码反而让 LLM 忽略关键问题？
- 不需要 LLM 的部分：证据锚定率置信度是否需要扩展变量名表

### 证据不足时 LLM 的自信度

A 模式（单行）没发现 bug，但 LLM 依然以同样自信的语气给出了"精度损失"分析。假设：**证据不足时 LLM 不会说"不知道"，而是编造一个看似合理的分析。** 这比空洞建议更危险——它看起来像真的。

可验证：给 LLM 残缺证据，统计编造分析的频率和置信度。


### 证据排序影响发现方向

C 模式给了反向追溯链（从 total 回溯到 raw）。如果给正向链路（raw → trimmed → parts → price → total），LLM 的关注点会不会不同？假设：**正向证据让 LLM 关注输入验证，反向证据让 LLM 关注值的使用。**

可验证：同一代码，同一粒度，正反两种排序，对比发现结果。


### LLM 误报识别和发现 bug 是同一能力

`sum += v` dismiss 和 `parts[2]` 发现用了同一个机制：LLM 综合上下文判断。如果成立，则 **`--llm` 不需要预设分析维度**——一个通用 prompt "这个 finding 对不对？还有什么问题？" 即可覆盖 dismiss 和升级两种场景。

可验证：在真实项目上，通用 prompt vs 专用 prompt 对比误报识别率和 bug 发现率。

## 待验证

- `reflect` 命令原型（定向分析，不跑 review）

## 假阳性过滤

LLM 在 `sum += v` 案例中成功识别了规则引擎的误报。这是 `--llm` 最明确的落地场景：降低噪声，不是增加洞察。可量化的实验：在真实项目上跑 review，统计 LLM dismiss 了多少 finding，人工验证 dismiss 是否合理。

## 不做什么

- 不做框架、不做容器、不做智能体
- 不做正式 CLI 发布的先决条件
