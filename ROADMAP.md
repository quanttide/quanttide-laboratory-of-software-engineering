# ROADMAP — qtcloud-code-cli（实验室）

## 定位

3R 代码审查 CLI：**review → reflect → refactor**。

本次实验重点：reflect 和 refactor 的**规则引擎**（机械部分，纯确定性）。

## P0 — 已实现 ✅

- [x] CLI 框架（`review` / `list-rules`）
- [x] tree-sitter 多语言集成（Rust / Python / Go / Dart / TypeScript）
- [x] 检测器：过长函数、unsafe 块、过长参数列表、未使用变量、缺失测试
- [x] 输出格式：终端 / JSON / STATUS.md
- [x] 配置系统：`.quanttide/code/contract.yaml` + `--rules`
- [x] 自举验证 + 77 测试 + 95% 覆盖率
- [x] 发布流水线：`cli/v*` tag → crates.io

## P1 — Reflect 规则引擎

在 review 基础上增加**机械侦探**能力。确定性，无 LLM。

### 程序切片
- [ ] 单函数反向切片：给定位置，找出所有影响该点的语句
- [ ] 跨函数切片：沿调用链追溯
- [ ] 切片输出：语句集合 + 控制流路径

### 数据流分析
- [ ] 变量级数据流：定义→使用路径追踪
- [ ] 跨函数数据流：参数传递、返回值追踪
- [ ] 数据流图输出：值传递路径

### 依赖图分析
- [ ] 项目模块依赖图构建
- [ ] 反向依赖切片：找出依赖某个模块的所有调用链
- [ ] 正向依赖切片：找出某个模块影响的所有下游

### 证据链合并
- [ ] 三种分析结果合并为统一证据链
- [ ] 证据链 JSON 输出

## P2 — Refactor 规则引擎

在 review + reflect 基础上增加**机械变换**能力。确定性，无 LLM。

### AST 变换基础设施
- [ ] AST 节点定位与边界识别
- [ ] 代码块选择与提取
- [ ] 符号表维护（定义→所有引用映射）

### 机械变换（IDE 级别）
- [ ] 函数提取：选中代码块 → 抽为新函数 + 替换调用点
- [ ] 符号重命名：找到所有引用 → 统一替换
- [ ] 内联变量：展开变量引用 → 删除原声明
- [ ] 内联函数：展开函数调用 → 删除原定义
- [ ] 死代码删除：检测未使用声明 → 移除

### 安全机制
- [ ] dry-run 默认（输出 diff 不写文件）
- [ ] `--apply` 显式确认
- [ ] 自动验证（编译 + 测试）
- [ ] 验证失败回退

## P3 — LLM 集成

在机械引擎完成后接入 LLM，作为增强层。

- [ ] LLM 二次审查：排序、去重、语义规则
- [ ] LLM 因果解释：在证据链上做根因分析
- [ ] LLM 策略选择：复杂 refactor 场景的代码生成
- [ ] `--mode lint / llm / deep` 三种模式
