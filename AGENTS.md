# AGENTS — qtcloud-code-cli

## 架构原则

- 纯规则引擎，不依赖 LLM
- 面向「可检测、可复现、可自动化」
- P0 阶段只做 Rust，再扩展多语言
- 做检测，不做自动修复

## 发现分级

遵循 RFC 2119 语义：

| 级别 | 含义 | 举例 |
|------|------|------|
| **MUST** | 可能引入 bug，必须审查 | 宽泛 unsafe 块 |
| **SHOULD** | 维护负担，建议重构 | 函数过长、参数过多 |
| **MAY** | 风格建议，可选采纳 | 命名约定 |

## 测试

```sh
cargo test
```

## 模块结构

```
src/
├── main.rs        # CLI 入口 (clap)
├── lib.rs         # 公开模块
├── lang/          # 语言解析器抽象与实现
├── detect/        # 检测器
└── report/        # 输出格式
```
